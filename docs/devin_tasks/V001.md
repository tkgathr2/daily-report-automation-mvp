# Devinタスク V001

## 目的（1行）
投稿完了画面に「もう一度日報を作成する」ボタンを追加する。

## 仕様参照（唯一の正）
docs/plan.md の「画面仕様」「処理フロー」「受け入れ条件」

## 変更してよいファイル
- src/Index.html のみ

## 受け入れ条件（3つ）
1. 投稿完了画面に「もう一度日報を作成する」ボタンが表示される
2. そのボタンを押すと、完了画面が消えて編集画面に戻れる
3. 既存の「Slackで確認する」ボタンや完了画面表示の挙動に影響しない

## NG
- docs/plan.md を変更しない
- src/Code.gs を変更しない
- 推測で機能を増やさない（仕様外のUI/挙動追加禁止）
- 依存関係を追加しない

## 完了条件
- git add . → commit → push
- Pull Request を作成
- PR本文に「受け入れ条件のチェック結果」をチェックリストで記載
