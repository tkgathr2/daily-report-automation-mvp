# Devin タスク: V6 実装

## これは Devin への指示です。

---

## タスク概要

簡単日報くん V6（AIによる自動処理）を実装する。

## 前提条件

- V5が完成・稼働していること

## 参照ドキュメント

- 要件定義書: `docs/specs/V6_requirements_ASK.md`
- 仕様書: `docs/specs/V6_plan.md`

---

## 実装手順

### Step 1: Code.gs に定数追加

```javascript
const PROPERTY_OPENAI_API_KEY = 'OPENAI_API_KEY';
const MAX_DISPLAY_ITEMS = 10;
```

### Step 2: Code.gs に関数追加

1. `sortByImportanceAI(items)` - AI重要度判定
2. `limitItems(items)` - 件数絞り込み
3. `getAllToolHistoryV6(dateString, useAI)` - V6統合取得

### Step 3: Index.html にUI追加

- AI ON/OFFチェックボックス
- 削除・編集可能なリストUI

### Step 4: Index.html のJavaScript追加

- `renderTaskList(items)` - リスト描画
- `makeEditable(element)` - 編集モード
- `deleteItem(element)` - 削除

---

## 完了条件

1. AIで重要度順に並べ替えられる
2. 最大10件に絞られる
3. 各項目を削除できる
4. 各項目を編集できる
5. V5機能が正常動作する

---

## 注意事項

- OpenAI API Key設定が必要
- API Keyがない場合は時間順で表示
- AI処理は10秒以内に完了すること
